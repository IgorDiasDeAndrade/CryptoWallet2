<p style="color: green"><%= notice %></p>
<div class="h100 p-5 bg-light rounded-3">
  <%= render @coin %>

  <div>
    <%= link_to t('links.edit'), edit_coin_path(@coin) %> |
    <%= link_to t('links.walletback'), coins_path %>
    <p></p>
    <%= button_to t('links.destroythiscoin'), @coin, method: :delete, class:"btn btn-dark btn-sm", role:"button" %>
  </div>
</div>